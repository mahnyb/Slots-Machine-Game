from tkinter import *

window = Tk() # an instance of a window

window.geometry("500x500")
window.title("mahny's coding adventures")

##icon = PhotoImage(file='pic.jpeg')
## window.iconphoto(True, icon)
window.config(background="pink")
window.mainloop() # displays the window and listens for events 